# Zoho Inventory API
- Base URL: https://inventory.zoho.com/api/v1
- Auth: OAuth2 (access token via refresh token)
- Create Sales Return: POST /salesreturns
- Credentials: client_id, client_secret, refresh_token, organization_id
